{
  "name": "yayamlls",
  "displayName": "yayamlls",
  "version": "0.1.0",
  "description": "YAML language server in Go: schema-driven diagnostics, completion, hover, and Flux rendering.",
  "categories": [
    "Linters",
    "Programming Languages"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/home-operations/yayamlls",
    "directory": "editors/vscode"
  },
  "publisher": "home-operations",
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  },
  "contributes": {
    "languages": [
      {
        "id": "yaml",
        "aliases": [
          "YAML",
          "yaml"
        ],
        "extensions": [
          ".yaml",
          ".yml"
        ]
      }
    ],
    "commands": [
      {
        "command": "yayamlls.restart",
        "title": "yayamlls: Restart Language Server"
      }
    ],
    "configuration": {
      "title": "yayamlls",
      "properties": {
        "yayamlls.path": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to a `yayamlls` binary. When set, the extension uses it instead of downloading a release."
        },
        "yayamlls.version": {
          "type": "string",
          "default": "latest",
          "description": "Release tag to download (e.g. \"0.0.5\"), or \"latest\"."
        },
        "yayamlls.catalog": {
          "type": "boolean",
          "default": true,
          "description": "Enable JSON Schema Store catalog lookups."
        },
        "yayamlls.catalogUrl": {
          "type": "string",
          "default": "",
          "description": "Override the schema catalog URL."
        },
        "yayamlls.schemas": {
          "type": "object",
          "default": {},
          "markdownDescription": "Map of schema URI/path → array of file globs. Mirrors the `schemas:` block in `.yayamlls.yaml`."
        },
        "yayamlls.kubernetes.schemaUrl": {
          "type": "string",
          "default": "",
          "markdownDescription": "Override the Kubernetes auto-detect URL template. Placeholders: `{group}`, `{groupSeg}`, `{kind}`, `{kindLower}`, `{version}`, `{versionLower}`, etc."
        },
        "yayamlls.flate.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Download [`flate`](https://github.com/home-operations/flate) and enable Flux `HelmRelease` / `Kustomization` rendering."
        },
        "yayamlls.flate.path": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to a `flate` binary. When set, skips the download."
        },
        "yayamlls.flate.version": {
          "type": "string",
          "default": "latest",
          "description": "flate release tag to download (e.g. \"0.1.32\"), or \"latest\"."
        },
        "yayamlls.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace LSP traffic between VS Code and yayamlls."
        }
      }
    }
  },
  "activationEvents": [
    "onLanguage:yaml"
  ],
  "engines": {
    "vscode": "^1.85.0"
  }
}
