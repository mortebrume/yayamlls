{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "embedded://yamlls/yamlls.schema.json",
  "title": "yamlls workspace configuration",
  "description": "Schema for .yamlls.yaml at the repo root.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "schemas": {
      "type": "object",
      "description": "Map of schema URL or filesystem path to one or more glob patterns. The first pattern that matches a document's path wins.",
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          { "type": "array", "items": { "type": "string" } }
        ]
      }
    },
    "catalog": {
      "type": "boolean",
      "description": "Whether to consult the JSON Schema Store catalog when other resolution steps don't match. Default: true."
    },
    "catalogUrl": {
      "type": "string",
      "description": "Override the catalog endpoint. Empty means use https://www.schemastore.org/api/json/catalog.json."
    },
    "kubernetes": {
      "type": "object",
      "additionalProperties": false,
      "description": "Tuning for per-document Kubernetes apiVersion+kind auto-detect.",
      "properties": {
        "schemaUrl": {
          "type": "string",
          "description": "URL template for Kubernetes schema lookup. Placeholders: {group}, {groupSeg}, {groupFirst}, {groupFirstSeg}, {kind}, {kindLower}, {version}, {versionLower}."
        }
      }
    },
    "renderers": {
      "type": "object",
      "description": "Per-renderer configuration. Keys match renderer names (e.g. 'flate'); values are renderer-specific.",
      "additionalProperties": {
        "type": "object"
      }
    }
  }
}
